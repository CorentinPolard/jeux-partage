{% for label, messages in app.flashes %}
{% for message in messages %}
<div class="flash-{{ label }}">
    <p class="flash">
        <strong>{{ message }}</strong>
    </p>
</div>
{% endfor %}
{% endfor %}